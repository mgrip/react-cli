// @flow strict

import * as React from "react";

declare export default (
  element: React.Node,
  callback?: () => void,
  width?: number,
  handler?: (output: string) => void,
  spacing?: string
) => void;
